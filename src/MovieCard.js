import React from "react";

const MovieCard = () => {
  return (
    <div className="MovieCardContainer">
      <img src="" />
      <button>Play</button>
    </div>
  );
};

export default MovieCard;
